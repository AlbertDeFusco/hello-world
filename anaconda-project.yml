name: HelloWorld

description: A sample anaconda-project with a runable command to serve a Panel application

packages:
  - python=3.7
  - panel

commands:
  default:
    unix: panel serve hello.py
    supports_http_options: True
